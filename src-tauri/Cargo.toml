[package]
name = "tail-burrow"
version = "0.3.2"
description = "Your cozy local home for online art favorites. Syncs, deduplicates, and organizes your collections from e621 and FurAffinity. "
authors = ["Elijah McLellan "]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "tail_burrow_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-opener = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
chrono = { version = "0.4", features = ["serde"] }
rusqlite = { version = "0.32", features = ["bundled"] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "blocking", "json", "cookies", "multipart"] }
md5 = "0.7"
tokio = { version = "1", features = ["time"] }
image = { version = "0.24", features = ["default", "webp"] }
keyring = "3"
sha2 = "0.10"
hex = "0.4"
rand = "0.8"
tauri-plugin-updater = "2.10.0"
aes-gcm = "0.10"
base64 = "0.22"
parking_lot = "0.12"
chromiumoxide = "0.9"
futures = "0.3"
